// Method 01.
var userNames = [""];
userNames.forEach(function (userNames) {
    if (userNames === "") {
        console.log("We need to find some users!");
    }
    else {
        // Greet users
    }
});
// Method 02.
var user_names = [];
userNames.forEach(function (userNames) {
    if (user_names.length === 0) {
        console.log("We need to find some users!");
    }
    else {
        // Greet users
    }
});
